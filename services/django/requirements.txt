Django==5.0.4
djangorestframework==3.15.1
django-cors-headers==4.3.1

# Not specifying which version will download the latest version
# Better to specify which version to avoid compatibility and unpredictability issues
python-decouple==3.1
psycopg2-binary==2.9.9